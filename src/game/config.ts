/** Ajusta vueltas (1–5). Luego vendrá del menú al crear sala. */
export const RACE_LAPS = 3;

/** Si estás más lejos del centro de la pista que esto, empieza el temporizador de reset. */
export const OFF_TRACK_MARGIN = 1.35;

/** Segundos fuera de pista antes de resetear al centro. */
export const OFF_TRACK_RESET_SEC = 2;
